twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        site_name: "Bien-Être"
        images_directory: 'http://localhost:8000/uploads/'
        logo_directory: 'http://localhost:8000/uploads/logo/'
        avatar_directory: 'http://localhost:8000/uploads/avatar/'
        promotion_directory: 'http://localhost:8000/uploads/promotions/'
        ## promotion_directory: '%kernel.project_dir%/public/uploads/promotions/'
        ## logo_directory: '%kernel.project_dir%/public/uploads/logo/'
        ## avatar_directory: '%kernel.project_dir%/public/uploads/avatar/'
        ## images_directory: '%kernel.project_dir%/public/uploads/'
        ## promotion_directory : '%kernel.project_dir%/public/uploads/promotions/'

when@test:
    twig:
        strict_variables: true
